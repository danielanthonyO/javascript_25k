// create a variable called count and set it to 0
// on the next line, add 1 to the count variable.
// print out the value of the count variable.
// Use console.log() to print the value of count. 

let count = 0;
count = count + 1;
console.log(count);

// count += 1;
// count++;

