/* Data types */

// ********************************************
// 1. create a variable called `age` and assign it a number (e.g. 25).
// code here ******
let age = 25;

// ********************************************
// 2. create a variable called `name` and assign it a string (e.g. "John").
// code here ********
const name = "John";

// ********************************************
// 3. create a variable called `isStudent` and assign it a boolean (e.g. true or false).
// code here ********
const isStudent = true;

// ********************************************
// 4. create a variable called `hobbies` and assign it an array with few strings strings.
// code here *****
let hobbies = ["reading", "swimming", "cooking"];

// ********************************************
// 5. create an object called `info` with two properties: schoolName (a string, e.g.,"Business College Helsinki") and groupName (a number, e.g. REACT25K).
// code here *****
let info = {
  schoolName: "Business College Helsinki",
  groupName: "REACT25K",
};
// 6. use console.log() to print out each variables to console.
console.log(age);
console.log(name);
console.log(isStudent);
console.log(hobbies);
console.log(info);


// 7. Use the typeof operator to check and print the data type of each variable.
console.log(typeof age);
console.log(typeof name);
console.log(typeof isStudent);
console.log(typeof hobbies);
console.log(typeof info);